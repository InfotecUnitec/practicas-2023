import { describe, it, expect } from "vitest";
import * as fs from "fs";

// LEER el archivo README.md dentro de la carpeta app

describe("infotec market", () => {
  it("productos", () => {});
  it("usuarios", () => {});
  it("carrito", () => {});
});
